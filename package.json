{
  "scripts": {
    "develop:backend": "npm --prefix backend run start:dev",
    "develop:frontend": "wait-on http://localhost:2222/ && npm --prefix frontend run start",
    "develop": "cross-env FORCE_COLOR=1 npm-run-all -l -p develop:*"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5",
    "wait-on": "^7.0.1"
  }
}
